<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.wallet</groupId>
        <artifactId>wallet</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>wallet-app</artifactId>

    <name>wallet-app</name>
    <description>启动模块：Spring Boot 入口、REST 接口、装配配置（Redisson/MyBatis/ChannelKit）、建表 SQL。</description>

    <dependencies>
        <dependency>
            <groupId>com.wallet</groupId>
            <artifactId>wallet-security</artifactId>
        </dependency>
        <dependency>
            <groupId>com.wallet</groupId>
            <artifactId>wallet-pay</artifactId>
        </dependency>
        <!-- 组合根：实现模块的 Bean 由 app 拉全。
             pay 已不依赖 account/channel，这里显式引入实现模块 + 契约，保证编译与运行时 Bean 齐备 -->
        <dependency>
            <groupId>com.wallet</groupId>
            <artifactId>wallet-account</artifactId>
        </dependency>
        <dependency>
            <groupId>com.wallet</groupId>
            <artifactId>wallet-channel</artifactId>
        </dependency>
        <dependency>
            <groupId>com.wallet</groupId>
            <artifactId>wallet-contract</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <!-- jakarta bean validation：@Valid 参数校验 -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <!-- 监控：/actuator/health、metrics -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <!-- Prometheus 抓取端点 /actuator/prometheus（JVM/CPU/GC/QPS/RT 指标） -->
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <!-- Redis 统一入口依赖；排除默认 Lettuce，底层由 RedissonConnectionFactory 接管 -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>io.lettuce</groupId>
                    <artifactId>lettuce-core</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson-spring-data-35</artifactId>
        </dependency>
        <dependency>
            <!-- @Lock4j 自动装配（库模块只依赖 lock4j-core 注解，装配统一在 app；RedissonLockExecutor 复用 RedisConfig 的 RedissonClient） -->
            <groupId>com.baomidou</groupId>
            <artifactId>lock4j-redisson-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <!-- MyBatis-Plus 自动装配（库模块只依赖 mp-core，starter 统一在装配层） -->
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>
        <dependency>
            <!-- MyBatis-Plus 分页插件依赖（PaginationInnerInterceptor 在 MybatisPlusConfig 装配） -->
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <!-- JUnit5 + Mockito + AssertJ 等 -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers：起真实 MySQL/Redis 做集成测试（版本由 Boot BOM 管） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mysql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
